//! Analysis configuration.
//!
//! ⚠️ STUB (Wave-2). `VisionConfig` already lives in `crate::models` (recovered),
//! and nothing currently references `analysis::config`. Wire analysis-specific
//! config here if a need emerges; otherwise this module stays empty and the
//! `pub mod config;` declaration in `analysis/mod.rs` may be dropped.
