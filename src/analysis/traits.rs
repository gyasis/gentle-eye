//! Analysis traits.
//!
//! ⚠️ STUB (Wave-2). The `VisionProvider` trait is defined in
//! `crate::contracts::traits` (recovered) and re-exported via `contracts`. This
//! module is a placeholder; define analysis-only traits here if needed, otherwise
//! the `pub mod traits;` declaration in `analysis/mod.rs` may be dropped.
