//! Pipe-encoder integration test. STUB — the FFmpeg pipe encoder is a Wave-3
//! synthesis target (`src/capture/encoder.rs`); this placeholder compiles cleanly
//! until the encoder is implemented, then the real dimension-mismatch / pipe tests
//! get restored here.
