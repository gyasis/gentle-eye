{
  "tasks": {
    "T021": {
      "mode": "native",
      "command": "T021 [US2] New `src/dayflow/scheduler.rs` (was T242) — a tokio task that summarizes each closed segment and writes its `TimelineEntry` immediately, channel-based and concurrency-safe with the capture loop (FR-014).",
      "status": "failed",
      "started_at": "2026-08-26T22:18:32.489130902Z",
      "completed_at": "2026-08-29T18:31:25.734804630Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T019": {
      "mode": "native",
      "command": "T019 [US1] Flesh `src/dayflow/daemon.rs` — continuous supervision, persisted lifecycle state, clean stop, and a restart that resumes onto the same day.",
      "status": "failed",
      "started_at": "2026-08-24T13:01:36.741161549Z",
      "completed_at": "2026-08-29T18:31:25.736378469Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T010": {
      "mode": "native",
      "command": "T010 Frame SAMPLER in `src/dayflow/sampler.rs` (supersedes T220's segment muxer per D9) — grab one frame per display at `sampling.interval_for(mode)`, persist it as a compressed still, and record it in the segment ledger. NO long-lived encoder: dayflow samples, it does not stream video. Delta-skip (D11) marks a sample unchanged rather than perceiving it.",
      "status": "failed",
      "started_at": "2026-08-24T10:26:31.033825267Z",
      "completed_at": "2026-08-24T10:27:07.489145562Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T022": {
      "mode": "native",
      "command": "T022 [US2] Retry, don't drop — a segment whose summarization fails stays `summarized = false` and is retried; capture keeps producing meanwhile.",
      "status": "failed",
      "started_at": "2026-08-26T22:18:32.492400250Z",
      "completed_at": "2026-08-29T18:31:25.737104772Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T023": {
      "mode": "native",
      "command": "T023 [US2] Return pause intervals as `gaps` alongside entries from `query_range` in `src/dayflow/timeline.rs`, so a gap is a recorded fact rather than missing data.",
      "status": "failed",
      "started_at": "2026-08-26T22:18:32.495088884Z",
      "completed_at": "2026-08-29T18:31:25.735563035Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T014": {
      "mode": "native",
      "command": "T014 [US1] Wire pause/resume into the rotation controller — pause closes the in-progress segment cleanly and records a `PauseInterval` with its cause; resume opens a new segment (FR-030/031/032).",
      "status": "failed",
      "started_at": "2026-08-24T12:00:29.262601148Z",
      "completed_at": "2026-08-24T12:02:12.326489335Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T005": {
      "mode": "native",
      "command": "T210 Duration-aware fps heuristic in `capture/frame_rate.rs` (≤30s→5–30; 30s–15min→1; 15min–hours→0.2–0.5) + **document it** in the `start_recording` MCP tool description AND `docs/FPS_AND_DAYFLOW.md`.",
      "status": "failed",
      "started_at": "2026-05-29T01:55:32.450975746Z",
      "completed_at": "2026-08-24T10:07:06.555230836Z",
      "constitution_rules": []
    },
    "T002": {
      "mode": "native",
      "command": "T201 [P] `src/dayflow/models.rs` — `TimelineEntry { recording_id: Uuid, start_time, end_time: DateTime<Utc>, category: ActivityCategory, app: String, activity: String, summary: String }`, `ActivityCategory` enum (Coding/Docs/Comms/Browsing/Meeting/Idle/Other), `ChunkSummary`, `RollingContext`, `DayflowSession`, `DayflowStatus`. Derive serde + schemars.",
      "status": "failed",
      "started_at": "2026-05-29T01:15:21.177307711Z",
      "completed_at": "2026-08-24T10:07:06.556049095Z",
      "constitution_rules": []
    },
    "T015": {
      "mode": "native",
      "command": "T015 [US1] Manual off/on in `src/dayflow/engine.rs` — turning capture on again the same day rejoins that day's session by `day` rather than opening a second timeline (FR-033).",
      "status": "failed",
      "started_at": "2026-08-24T12:00:29.264712482Z",
      "completed_at": "2026-08-24T12:02:12.328740937Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T008": {
      "mode": "native",
      "command": "T222 Chunking integration — a simulated/short 35-min-equivalent capture yields 3 chunks with correct boundaries.",
      "status": "failed",
      "started_at": "2026-05-29T01:58:44.573592305Z",
      "completed_at": "2026-08-24T10:07:06.556680826Z",
      "constitution_rules": []
    },
    "T011": {
      "mode": "native",
      "command": "T011 Sample-window controller in `src/capture/service.rs` — closes a window on the segment boundary, records its ACTUAL `start_wall`/`end_wall` and sample count in the ledger, and handles interval change, pause, resume and display change. Optional timelapse assembly (`video.enabled`, default off) invokes ffmpeg ONCE per window over the collected stills, using the argument vector proved in T004.",
      "status": "failed",
      "started_at": "2026-08-24T10:26:31.035767434Z",
      "completed_at": "2026-08-24T10:27:07.491498734Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T001": {
      "mode": "native",
      "command": "T200 [P] `src/dayflow/mod.rs` — module root: `pub mod {models, engine, summarizer, timeline, retention, daemon}` + re-exports. Wire `pub mod dayflow;` into `src/lib.rs`.",
      "status": "failed",
      "started_at": "2026-05-29T01:15:21.172091422Z",
      "completed_at": "2026-08-24T10:07:06.555409611Z",
      "constitution_rules": []
    },
    "T006": {
      "mode": "native",
      "command": "T220 Extend `capture/encoder.rs` / `capture/service.rs` for **segmented capture** — ffmpeg segment muxer emitting 15-min chunk files on the fly (mirror videolocr `download_and_split_video`). Chunk index + wall-clock start/end recorded.",
      "status": "failed",
      "started_at": "2026-05-29T01:58:44.566155833Z",
      "completed_at": "2026-08-24T10:07:06.556364696Z",
      "constitution_rules": []
    },
    "T013": {
      "mode": "native",
      "command": "T013 [P] [US1] New `src/dayflow/idle.rs` — an `IdleDetector` trait plus the X11 screensaver backend from T005, with hysteresis on both transitions. A platform with no backend reports \"never idle\".",
      "status": "failed",
      "started_at": "2026-08-24T12:00:29.259860431Z",
      "completed_at": "2026-08-24T12:02:12.327682700Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T020": {
      "mode": "native",
      "command": "T020 [P] [US1] Integration coverage in `tests/dayflow_segmentation.rs` — boundaries, non-uniform lengths, pause gaps, clock discontinuity, display hot-plug.",
      "status": "failed",
      "started_at": "2026-08-24T13:01:36.743174319Z",
      "completed_at": "2026-08-29T18:31:25.737923373Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T016": {
      "mode": "native",
      "command": "T016 [US1] Apply a segment-interval change at the next boundary without re-timing any existing entry (FR-034/035); persist the interval in force on the session.",
      "status": "failed",
      "started_at": "2026-08-24T12:00:29.266852230Z",
      "completed_at": "2026-08-24T12:02:12.328038090Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T003": {
      "mode": "native",
      "command": "T202 [P] `src/dayflow/errors.rs` (or extend `contracts/errors.rs`) — `DayflowError` enum + `From` convs + `mcp_error_code()` mapping (mirror `GentleEyeError`).",
      "status": "failed",
      "started_at": "2026-05-29T01:15:21.182437892Z",
      "completed_at": "2026-08-24T10:07:06.555585031Z",
      "constitution_rules": []
    },
    "T007": {
      "mode": "native",
      "command": "T221 [P] Chunk manifest — `ChunkRef { index, path, start_wall, end_wall }` emitted per segment; `MemoryMonitor` integration (file-based encoder under pressure, reuse `capture/memory.rs`).",
      "status": "failed",
      "started_at": "2026-05-29T01:58:44.570258344Z",
      "completed_at": "2026-08-24T10:07:06.555731821Z",
      "constitution_rules": []
    },
    "T018": {
      "mode": "native",
      "command": "T018 [US1] Flesh `src/dayflow/engine.rs` — `start_session` / `stop_session` / `status` over one shared pipeline, honouring the session max-duration cap and closing the in-progress segment on stop (FR-003/005).",
      "status": "failed",
      "started_at": "2026-08-24T13:01:36.738807969Z",
      "completed_at": "2026-08-29T18:31:25.735978387Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T012": {
      "mode": "native",
      "command": "T012 [US1] Per-display capture pipelines in `src/capture/service.rs`, one encoder per display via the existing `DisplayManager`, all writing into one session (FR-029).",
      "status": "failed",
      "started_at": "2026-08-24T12:00:29.255494669Z",
      "completed_at": "2026-08-24T12:02:12.328383682Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T017": {
      "mode": "native",
      "command": "T017 [US1] `DayflowLiveness` in `src/dayflow/models.rs` + `daemon.rs`, every field read from the segment ledger and `timeline_entries` — never from an in-memory flag (FR-006/008).",
      "status": "failed",
      "started_at": "2026-08-24T12:00:29.269045208Z",
      "completed_at": "2026-08-24T12:02:12.326083969Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T025": {
      "mode": "native",
      "command": "T025 [P] [US2] Integration coverage in `tests/dayflow_timeline.rs` — live-write ordering, range queries, gaps, empty-range grounding.",
      "status": "failed",
      "started_at": "2026-08-26T22:18:32.497571207Z",
      "completed_at": "2026-08-29T18:31:25.737523652Z",
      "constitution_rules": [
        "Edition **2021** (not 2024).",
        "All deps pinned in `Cargo.toml` — no unspecified versions.",
        "Capture via **`scrap = \"0.5\"`** (not `scap`).",
        "MCP server via **`rmcp = \"0.1\"`** with features `[\"server\"",
        "\"macros\"]`.",
        "Errors: **`thiserror = \"1\"`** for typed; **`anyhow = \"1\"`** for app errors.",
        "Vision providers via **`reqwest = \"0.12\"`** with `[\"json\"]`.",
        "Config via **`config = \"0.14\"`** + **`toml = \"0.8\"`** + **`directories = \"5\"`**.",
        "`pub` items get a `///` doc comment.",
        "Each module has `#[cfg(test)] mod tests` covering constructors + validation.",
        "No hardcoded secrets; env vars only (`GEMINI_API_KEY`",
        "etc.).",
        "No `unwrap()` outside tests; use `?` + `thiserror` types.",
        "`analyze_rate_limit_per_minute = 10`",
        "enforced by `src/security/rate_limiter.rs`.",
        "All paths validated by `src/security/path_validator.rs` against `allowed_video_paths`.",
        "Recording IDs are UUID v4",
        "validated by `src/security/uuid_validator.rs`.",
        "**`cargo check --message-format=short`** must pass at every wave checkpoint.",
        "`cargo clippy -- -D warnings` before any merge to main.",
        "`cargo test` green at Wave 10.",
        "Halt-and-fix: tool bug = the valued finding. Stop",
        "capture",
        "fix the TOOL.",
        "Path-seed over session-gating (R-DR9).",
        "Check existing tools before reinventing (R-DR10).",
        "Tool dumps are data",
        "not noise (R-DR11).",
        "`coverage_pct=100 COMPLETE` can be empty — verify byte size (R-DR12)."
      ]
    },
    "T004": {
      "mode": "native",
      "command": "T203 Extend `src/config/mod.rs` — `DayflowConfig { chunk_minutes: 15, record_fps: 0.5, default_provider: Gemini, retention: RetentionConfig, disk_budget_bytes }`; sane serde defaults.",
      "status": "failed",
      "started_at": "2026-05-29T01:15:21.188324016Z",
      "completed_at": "2026-08-24T10:07:06.556517178Z",
      "constitution_rules": []
    }
  }
}